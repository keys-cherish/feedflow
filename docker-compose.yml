services:
  feedflow:
    build: .
    ports:
      - "3200:3200"
    volumes:
      - feedflow-data:/data
    restart: unless-stopped
    environment:
      - FEEDFLOW_LOG_LEVEL=info

volumes:
  feedflow-data:
